import { Typography, Box } from '@mui/material';

const Games = () => (
  <Box>
    <Typography variant="h4">Equipos</Typography>
    <Typography>Lista de juegos próximamente...</Typography>
  </Box>
);

export default Games;
